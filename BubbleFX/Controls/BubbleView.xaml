<UserControl x:Class="VisualBuffer.BubbleFX.Controls.BubbleView"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
mc:Ignorable="d" MinWidth="220" MinHeight="120">
    <Border x:Name="Root" CornerRadius="12" Background="#1A1F2B" BorderBrush="#2E3440" BorderThickness="1" >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>


            <!-- Header / drag handle -->
            <Grid x:Name="Header" Grid.Row="0" Background="#232938" Cursor="SizeAll">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Title}" Margin="12,0,0,0" VerticalAlignment="Center" Foreground="#E6EEF7"/>
                <Button Grid.Column="1" x:Name="CloseBtn" Content="✕" Width="28" Height="24" Margin="0,6,6,6"
ToolTip="Close"
VerticalAlignment="Center"/>
            </Grid>


            <!-- Content placeholder -->
            <Border Grid.Row="1" Background="#0F1116" CornerRadius="0,0,12,12" Padding="12">
                <TextBlock Foreground="#C8D0E0"
               TextWrapping="Wrap"
               Text="{Binding ContentText}"/>
            </Border>

        </Grid>
    </Border>
</UserControl>