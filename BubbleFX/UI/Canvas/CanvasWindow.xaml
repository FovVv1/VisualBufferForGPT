<Window x:Class="VisualBuffer.BubbleFX.UI.Canvas.CanvasWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ResizeMode="NoResize"
        Topmost="True"
        Background="#CC111111"
        AllowsTransparency="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        Width="1200"
        Height="800"
        WindowStartupLocation="CenterScreen"
        AllowDrop="True"
        Drop="OnDrop"
        DragOver="OnDragOver">
    <Grid Margin="24">
        <DockPanel>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,8">
                <TextBlock Text="Холст" Foreground="White" FontSize="16" Margin="0,0,12,0"/>
                <Button Content="Закрыть" Width="100" Click="OnCloseClick"/>
            </StackPanel>

            <ScrollViewer>
                <ItemsControl x:Name="CardsPanel">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Click="OnCardClick" Tag="{Binding}" Margin="6" Padding="8">
                                <StackPanel>
                                    <TextBlock Text="{Binding}" FontFamily="Consolas"
                             Foreground="White" TextWrapping="Wrap"
                             MaxWidth="520" MaxHeight="160"/>
                                    <TextBlock Text="Взять" Foreground="#FF9CDCFE"
                             HorizontalAlignment="Right" />
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>
        </DockPanel>
    </Grid>
</Window>
